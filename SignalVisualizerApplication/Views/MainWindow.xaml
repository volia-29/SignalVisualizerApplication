<Window x:Class="SignalVisualizerApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SignalVisualizerApplication"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="MainWindow" Height="450" Width="800" MinWidth="600" MinHeight="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Start" Command="{Binding StartCommand}"/>
            <Button Content="Stop" Command="{Binding StopCommand}" Margin="10 0"/>
            <TextBlock Text="Filter:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Delay=1000}" 
                     Height="25" Width="100" Margin="10 0"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding RecordsView}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Created On" Binding="{Binding Timestamp, StringFormat=\{0:dd MMM yyyy HH:mm:ss\}}" Width="*" />
                <DataGridTextColumn Header="Frequency, MHz" Binding="{Binding Frequency, StringFormat=\{0:F1\}}" Width="*" />
                <DataGridTextColumn Header="Bandwidth, kHz" Binding="{Binding Bandwidth, StringFormat=\{0:F3\}}" Width="*" />
                <DataGridTextColumn Header="SNR, dB" Binding="{Binding SNR, StringFormat=\{0:F3\}}" Width="*" />
                <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
